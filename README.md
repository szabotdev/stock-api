# stock-api

## Development

- Install dependencies: `make install`
- Run the app: `python run.py`
- Run the app through the venv: `make run`
- Run the tests: `make test`

The Makefile targets use the local virtualenv at `.venv/` and run the app or `pytest` directly from there.
